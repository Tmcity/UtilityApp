<Window x:Class="PowerGuard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PowerGuard" Height="480" Width="380" 
        Background="Transparent"
        WindowStyle="None" 
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        WindowStartupLocation="CenterScreen"
        MinHeight="450" MinWidth="350">
    
    <!-- 定义全局资源和样式 -->
    <Window.Resources>
        
        <!-- 通用文本框样式 -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#3A3A3A"/>
            <Setter Property="Foreground" Value="#FFF"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                         Padding="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 文本块样式 -->
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,8,0,3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
        </Style>

        <!-- 复选框样式 -->
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,8,0,3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="CheckBoxBorder" 
                                   Width="16" Height="16" 
                                   Background="#3A3A3A" 
                                   BorderBrush="#666" 
                                   BorderThickness="1" 
                                   CornerRadius="2"
                                   VerticalAlignment="Center">
                                <Path x:Name="CheckMark" 
                                     Data="M 2,5 L 6,8 L 12,2" 
                                     Stroke="#007ACC" 
                                     StrokeThickness="2" 
                                     Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="6,0,0,0" 
                                            VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="#004A70"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 标题栏按钮样式 -->
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#404040"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#303030"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 关闭按钮特殊样式 -->
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#C50E1F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 状态标签样式 -->
        <Style x:Key="StatusLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FFF"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
        </Style>

        <!-- 分组标题样式 -->
        <Style x:Key="GroupTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#007ACC"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- 自定义滚动条样式 -->
        <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border Background="#646465" CornerRadius="3"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="HorizontalScrollBarPageButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Opacity" Value="0.2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Rectangle Fill="{TemplateBinding Background}" 
                                  Width="{TemplateBinding Width}" 
                                  Height="{TemplateBinding Height}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="VerticalScrollBarPageButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Opacity" Value="0.2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Rectangle Fill="{TemplateBinding Background}" 
                                  Width="{TemplateBinding Width}" 
                                  Height="{TemplateBinding Height}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="VerticalScrollBarPageButton2" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle"  Value="true"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Opacity"  Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Rectangle Fill="#90000000" Width="0" Height="0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="CustomScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="0,1,1,6"/>
            <Setter Property="Width"  Value="6"/>
            <Setter Property="MinWidth"  Value="6"/>
            <Setter Property="Opacity"   Value="0.3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <RepeatButton Grid.Row="0" 
                                        Style="{StaticResource VerticalScrollBarPageButton2}" 
                                        Command="{x:Static ScrollBar.PageUpCommand}"/>
                            <Track x:Name="PART_Track" Grid.Row="1" 
                                  IsEnabled="{TemplateBinding IsMouseOver}" 
                                  IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource VerticalScrollBarPageButton}" 
                                                Command="{x:Static ScrollBar.PageUpCommand}"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource VerticalScrollBarPageButton}" 
                                                Command="{x:Static ScrollBar.PageDownCommand}"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                            <RepeatButton Grid.Row="2" 
                                        Style="{StaticResource VerticalScrollBarPageButton2}" 
                                        Command="{x:Static ScrollBar.PageDownCommand}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 默认滚动条样式 -->
        <Style TargetType="ScrollBar" BasedOn="{StaticResource CustomScrollBarStyle}"/>

    </Window.Resources>

    <!-- 设置窗口样式和布局 -->
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5"/>
    </WindowChrome.WindowChrome>

    <!-- 主容器 -->
    <Border Background="#2D2D2D" 
            BorderBrush="#555555" 
            BorderThickness="1" 
            CornerRadius="6">
        
        <Grid ClipToBounds="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 自定义标题栏 -->
            <Grid Background="#1E1E1E" Grid.Row="0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 应用标题和图标 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" 
                           VerticalAlignment="Center" Margin="10,0,0,0">
                    <TextBlock Text="🛡️" FontSize="14" Foreground="#007ACC" 
                              VerticalAlignment="Center" Margin="0,0,6,2"/>
                    <TextBlock Text="PowerGuard" 
                              VerticalAlignment="Center" 
                              Foreground="#E0E0E0" 
                              FontFamily="Microsoft YaHei" 
                              FontSize="12"
                              FontWeight="SemiBold" Margin="0,0,0,0"/>
                </StackPanel>
                
                <!-- 标题栏按钮 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Style="{StaticResource CloseButtonStyle}" 
                           Content="✕"
                           Width="34" Height="28" 
                           Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>

            <!-- 主内容区域 -->
            <ScrollViewer Grid.Row="1" 
                         VerticalScrollBarVisibility="Auto" 
                         HorizontalScrollBarVisibility="Disabled"
                         Margin="0">
                <StackPanel Margin="12,10,12,12">
                    
                    <!-- 网络设置组 -->
                    <Border Background="#353535" 
                           BorderBrush="#555555" 
                           BorderThickness="1" 
                           CornerRadius="4" 
                           Padding="12" 
                           Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="🌐 网络设置" 
                                      Style="{StaticResource GroupTitleStyle}" Margin="0,0,0,0"/>
                            
                            <!-- 网络设置：两列布局 -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 第一行：网关地址和检查间隔 -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="网关地址:" />
                                <TextBox Name="GatewayAddressTextBox" 
                                        Grid.Row="1" Grid.Column="0"
                                        Text="192.168.1.1" 
                                        TextChanged="AutoSave_TextChanged"/>

                                <TextBlock Grid.Row="0" Grid.Column="2" Text="检查间隔 (秒):" />
                                <TextBox Name="CheckIntervalTextBox" 
                                        Grid.Row="1" Grid.Column="2"
                                        Text="10" 
                                        TextChanged="AutoSave_TextChanged"/>

                                <!-- 第二行：停电容忍时间和关机倒计时时长 -->
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="停电容忍时间 (秒):" />
                                <TextBox Name="ShutdownThresholdTextBox" 
                                        Grid.Row="3" Grid.Column="0"
                                        Text="60" 
                                        TextChanged="AutoSave_TextChanged"/>

                                <TextBlock Grid.Row="2" Grid.Column="2" Text="关机倒计时时长 (秒):" />
                                <TextBox Name="ShutdownCountdownTextBox" 
                                        Grid.Row="3" Grid.Column="2"
                                        Text="120" 
                                        TextChanged="AutoSave_TextChanged"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 功能设置组 -->
                    <Border Background="#353535" 
                           BorderBrush="#555555" 
                           BorderThickness="1" 
                           CornerRadius="4" 
                           Padding="12" 
                           Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="🔧 功能设置" 
                                      Style="{StaticResource GroupTitleStyle}"/>

                            <!-- 功能设置：三个选项在一行 -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- 第一列：启用检测 -->
                                <CheckBox Name="EnableCheckBox" 
                                         Grid.Column="0"
                                         Content="启用检测" 
                                         IsChecked="True" 
                                         Checked="AutoSave_Checked" 
                                         Unchecked="AutoSave_Checked"
                                         Margin="0,4,0,4"/>

                                <!-- 第二列：开机自启 -->
                                <CheckBox Name="AutoStartCheckBox" 
                                         Grid.Column="2"
                                         Content="开机自启"
                                         Margin="0,4,0,4"/>

                                <!-- 第三列：调试模式 -->
                                <CheckBox Name="DebugCheckBox" 
                                         Grid.Column="4"
                                         Content="调试模式" 
                                         IsChecked="False" 
                                         Checked="AutoSave_Checked" 
                                         Unchecked="AutoSave_Checked"
                                         Margin="0,4,0,4"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 状态信息组 -->
                    <Border Background="#353535" 
                           BorderBrush="#555555" 
                           BorderThickness="1" 
                           CornerRadius="4" 
                           Padding="12">
                        <StackPanel>
                            <TextBlock Text="📊 状态信息" 
                                      Style="{StaticResource GroupTitleStyle}"/>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="当前状态: " 
                                          Foreground="#CCCCCC" 
                                          FontFamily="Microsoft YaHei" Margin="0,0,0,0"/>
                                <TextBlock Name="StatusTextBlock" 
                                          Text="未开始" 
                                          Style="{StaticResource StatusLabelStyle}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBox Name="LogTextBox" 
                                    Text="" 
                                    IsReadOnly="True" 
                                    AcceptsReturn="True" 
                                    Background="#2A2A2A" 
                                    Foreground="#E0E0E0"
                                    FontFamily="Consolas"
                                    FontSize="10"
                                    Margin="0" 
                                    Height="85"
                                    VerticalContentAlignment="Top"
                                    TextWrapping="Wrap"
                                    VerticalScrollBarVisibility="Auto"
                                    HorizontalScrollBarVisibility="Disabled"
                                    Padding="0"
                                    BorderThickness="0"
                                    BorderBrush="Transparent" FontWeight="Bold">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="TextBox">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            BorderBrush="#555" 
                                                            BorderThickness="1" 
                                                            CornerRadius="3">
                                                        <ScrollViewer x:Name="PART_ContentHost" 
                                                                     Padding="0"
                                                                     Margin="0"
                                                                     VerticalAlignment="Stretch"
                                                                     HorizontalAlignment="Stretch"
                                                                     VerticalContentAlignment="Top"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>    
</Window>